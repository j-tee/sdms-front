import React from 'react'

const ContinuousAssessmentCard = (props: any) => {
    const {schoolId, branchId, index} = props;
  return (
    <div>
      <h1>Continuous Assessment Card </h1>
    </div>
  )
}

export default ContinuousAssessmentCard
